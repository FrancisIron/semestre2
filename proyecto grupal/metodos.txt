clase Producto
int precio
String codigo
String nombre
int cantidad

producto(int newprecio,String newcodigo,String newnombre,int newcantidad)
{
  precio=newprecio;
  codigo=newcodigo;
  nombre=newnombre;
  cantidad=newcantidad;
}

clase Inventario
arraylist<Producto> array = new arraylist<Producto>();

crearProducto(int,String,String,int)
{
  Producto prod = new Porducto(int,String,String,int);
  array.add(prod);
}

vender(int i, int cant){
  prod.get(i).setCantidad(prod.get(i).getCantidad()-cant);
}
______________________________________________
import java.io.File;
import java.io.FileWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.List;
 
import com.csvreader.CsvWriter;   
        
        guardarCsv(){
        String outputFile = "test/productos.csv";
        boolean alreadyExists = new File(outputFile).exists();
         
        if(alreadyExists){
            File ficheroProductos = new File(outputFile);
            ficheroProductos.delete();
        }        
         
        try {
         
            CsvWriter csvOutput = new CsvWriter(new FileWriter(outputFile, true), ',');
             
            csvOutput.write("Nombre");
            csvOutput.write("Código");
            csvOutput.write("Precio");
            csvOutput.write("Cantidad");
            csvOutput.endRecord();
             
            for(int i=0;i<array.size();i++){
                 
                csvOutput.write(array.get(i).getNombre());
                csvOutput.write(array.get(i).getCodigo());
                csvOutput.write(array.get(i).getPrecio());
                csvOutput.write(array.get(i).getCantidad());
                csvOutput.endRecord();                   
            }
             
            csvOutput.close();
         
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
